/**
 * Обои внутри приложения — мягкие градиентные пятна под всем интерфейсом.
 *
 * Здесь нечего рендерить, кроме самого слоя: и набор оттенков, и их плотность
 * заданы в CSS по атрибуту data-wallpaper на <html>. Так вариант успевает
 * примениться до первой отрисовки, вместе с темой и акцентом, и не мигает.
 */
export function Wallpaper() {
  return <div className="wallpaper" aria-hidden />;
}

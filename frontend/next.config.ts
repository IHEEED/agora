import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /**
   * Открытие приложения с телефона по адресу вида 192.168.x.x:3000.
   *
   * Next.js 16 по умолчанию блокирует свои dev-ресурсы (HMR, чанки) для любого
   * источника, кроме localhost. Разметка при этом отдаётся с кодом 200, а
   * скрипты — нет: страница не гидрируется и остаётся чёрной. Разрешаем
   * частные диапазоны, чтобы адрес не приходилось править после каждой
   * перевыдачи адреса роутером. На продакшен-сборку не влияет — только на dev.
   */
  allowedDevOrigins: ["192.168.*.*", "10.*.*.*", "172.16.*.*"],
};

export default nextConfig;
